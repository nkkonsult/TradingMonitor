% ---------------------------------------------------------------------
%  Chapitre 2 — Les données et leur structure de dépendance
%  Principe du chapitre : décrire les données PUIS mesurer, AVANT tout
%  test, les trois canaux par lesquels les observations se ressemblent.
%  (Le chap. 3 s'appuiera sur ces mesures : « la menace quantifiée en
%  §2.3 »). Réflexe du cours de sondages : connaître son plan
%  d'échantillonnage avant d'estimer.
% ---------------------------------------------------------------------
\chapter{Les données et leur structure de dépendance}
\label{chap:donnees}

\section{La base des trades}
\label{sec:donnees-trades}

% [À RÉDIGER — présentation de la matière première, 8-12 lignes]
% Idées à développer avec TES mots :
%  - Provenance : le moteur de stratégies de l'agent développé en stage
%    (10 vraies stratégies d'analyse technique + 1 oracle témoin, titres
%    du S&P 500, 2010->2026).
%    Chaque LIGNE = un trade fermé (une position ouverte puis clôturée
%    par une stratégie sur un titre) -> unité statistique du chap. 3.
%  - Volumétrie : 40 362 trades exploitables (dont oracle 2 152), 501 titres.
%  - Présenter le tableau des variables (tab:dico-trades ci-dessous) :
%    distinguer les qualitatives (strategy, sector, regime) des
%    quantitatives de contexte (vol, rsi, dist_ma200, holding) et des
%    variables de résultat (return_net, edge, win).
%  - IMPORTANT (sert le chap. 3) : signaler dès ici que `ticker` est une
%    GRAPPE naturelle (plusieurs trades par titre) et que `entry/exit_date`
%    ouvrent la fenêtre d'exposition (des trades qui DURENT) -> ce sont
%    les deux ingrédients de la dépendance mesurée en §2.3. Ne pas encore
%    la traiter, juste la semer.

Tout au long de ce rapport, on appliquera les méthodes à notre base
de données. Celle-ci a été obtenue en appliquant différentes stratégies
sur des données antérieures. Cette méthode se nomme le \emph{backtesting}, et
consiste à vérifier si notre stratégie a repéré un signal de prédiction
du marché.

Ici nous appliquons 10 stratégies techniques classiques ainsi qu'une stratégie
oracle témoin.
La stratégie oracle utilise les données futures pour effectuer des
trades uniquement gagnants, afin de vérifier si notre protocole laisse passer
une stratégie dont le signal fonctionne réellement.
Les 10 autres stratégies, elles, effectuent un trade à une date fixée en
utilisant uniquement les données précédant cette date.

J'ai alors choisi d'appliquer ces stratégies de trading aux
501 grandes capitalisations américaines présentes dans le S\&P~500.
Le S\&P~500 est un ensemble de 501 titres sur lequel on peut investir avec peu de
risque~: même si le cours de chacune de ces actions peut chuter
individuellement, l'indice lui-même reste relativement stable et possède une tendance
haussière.

Pour qu'une stratégie soit validée, il faut qu'elle ait de meilleurs
résultats qu'une stratégie au hasard et que cela ne soit pas du a 
notre échantillonnage.
Ici la tendance est haussière, ce qui nous amène à la première
difficulté mentionnée dans l'introduction~: comment évaluer une 
stratégie lorsque la tendance est haussière ?

Pour répondre à cette problématique, on va simuler le hasard.
À chaque trade réalisé, on associe 200~trades fictifs de même exposition
(même titre, même durée, même sens), mais dont la date
d'entrée est tirée au sort~; la moyenne de leurs rendements définit le
gain du hasard pour ce trade.

On définit alors, pour chaque trade, son \edgevar{} comme le rendement
effectivement obtenu par la stratégie (éventuellement négatif) diminué de
ce gain du hasard~:
\[
  \edgevar = \text{gain de la stratégie} - \text{gain du hasard}.
\]
Un \edgevar{} positif signifie que la stratégie a fait mieux qu'une entrée
au hasard de même exposition~: son \emph{timing} apporte une information
que la seule dérive du marché n'explique pas.

On obtient ainsi une base de données contenant, pour chaque stratégie,
entre 978 et 8\,513 trades effectués entre janvier~2010 et juillet~2026,
pour un total de 40\,362 trades.
Dans nos données, chaque ligne correspond à une position ouverte puis fermée par une
stratégie sur un titre.
On y associe les indications suivantes~: le titre
(\emph{ticker}), la stratégie employée, le secteur, les dates d'entrée et de
sortie, la durée de détention, le régime de marché et le contexte à l'entrée
(volatilité, RSI, distance à la moyenne mobile), le rendement et l'avantage
(\edgevar{}) sur un tirage aléatoire équivalent.

\begin{table}[H]
\centering
\caption{Description des variables d'un trade, illustrées sur un trade réel
de la base (achat du titre \texttt{A} par la stratégie \texttt{rsi\_classic}).}
\label{tab:dico-trades}
\renewcommand{\arraystretch}{1.3}
\small
\begin{tabular}{@{}p{2.4cm}p{2.6cm}p{5.6cm}l@{}}
\toprule
Variable & Nature & Signification & Exemple \\
\midrule
\texttt{ticker}       & qualitative (501 mod.) & le titre traité (une même action revient dans plusieurs trades) & \texttt{A} \\
\texttt{strategy}     & qualitative (11 mod.)  & la stratégie ayant produit le trade & \texttt{rsi\_classic} \\
\texttt{sector}       & qualitative (11 mod.)  & le secteur d'activité du titre & Santé \\
\texttt{entry\_date}  & date                   & date d'achat & 2011-08-11 \\
\texttt{exit\_date}   & date                   & date de revente & 2012-01-20 \\
\texttt{holding\_days}& quantitative           & durée de détention, en jours & $162$ \\
\texttt{regime\_entry}& qualitative            & régime du marché à l'entrée & baissier \\
\texttt{vol\_entry}   & quantitative           & volatilité du titre à l'entrée & $0{,}043$ \\
\texttt{rsi\_entry}   & quantitative           & niveau de l'indicateur RSI à l'entrée & $30{,}1$ \\
\texttt{dist\_ma200}  & quantitative           & écart à la moyenne mobile sur 200 jours & $-0{,}210$ \\
\texttt{return\_net}  & quantitative           & rendement net réalisé par la stratégie & $+0{,}187$ \\
\texttt{rand\_return} & quantitative           & rendement d'une entrée au hasard équivalente & $+0{,}078$ \\
\texttt{edge}         & quantitative           & \textbf{avantage} = \texttt{return\_net} $-$ \texttt{rand\_return} & $+0{,}109$ \\
\texttt{win}          & binaire                & trade gagnant ($1$) ou perdant ($0$) & $1$ \\
\bottomrule
\end{tabular}
\end{table}



% NOTE : la construction de rand_return / edge est déjà rédigée plus haut
% (paragraphe « Pour répondre à cette problématique… »). L'ancien bloc-trame
% redondant a été retiré pour éviter d'expliquer l'edge deux fois.

\section{Les rendements sectoriels}
\label{sec:donnees-secteurs}

% [À RÉDIGER — court, 4-6 lignes : c'est une donnée SECONDAIRE]
% Idées à développer avec TES mots :
%  - Nature : 11 séries de rendements quotidiens sectoriels + un indice
%    de marché agrégé, ~4 132 jours de cotation. (Distinct de la base des
%    trades : ici on observe le marché lui-même, pas les positions.)
%  - À quoi ça sert (annoncer les 2 usages, sans les traiter) :
%    (a) mesurer la part COMMUNE des mouvements = le canal 2 de la
%        dépendance (§\ref{sec:dep-mesuree}) ;
%    (b) éclairer le verdict du chap. 3 (le marché est-il prévisible par
%        son passé ? -> lecture du verdict).
%  - Ne pas détailler les résultats ici (facteur marché, ARIMA) : ils
%    viennent au §2.3 et au chap. 3.
En complément de la base des trades, on dispose de \textbf{11 séries de
rendements quotidiens sectoriels} (plus un indice de marché agrégé)
observées sur environ 4\,132 jours de cotation. Elles serviront à
mesurer la part \emph{commune} des mouvements du marché
(section~\ref{sec:dep-mesuree}) puis à éclairer le verdict du
chapitre~\ref{chap:juger}.

\section{La structure de dépendance, mesurée}
\label{sec:dep-mesuree}

% [À RÉDIGER — chapeau de la section, 5-8 lignes] Idées :
%  - Les tests du chap. 3 traiteront chaque trade comme un témoin. Or un
%    témoignage ne vaut que s'il est indépendant des autres. Avant de
%    tester, on fait donc le bilan : PAR OÙ les trades peuvent-ils se
%    ressembler ?
%  - C'est le réflexe du cours de sondages : décrire le plan
%    d'échantillonnage (ici : des trades qui arrivent en grappes par
%    titre, en vagues par date, et qui se chevauchent dans le temps)
%    AVANT de calculer la moindre erreur-type.
%  - Annonce des trois canaux et de leur mesure, chacun par une méthode
%    de cours : ICC (ANOVA), corrélations + ACP, ACF.

% --- Canal 1 : les grappes par titre ---------------------------------
\subsection{Canal 1 --- les grappes par titre}
\label{sec:dep-canal1}

% [À RÉDIGER] Idées :
%  - Deux trades sur le même titre partagent la vie de ce titre (même
%    société, mêmes chocs spécifiques) : la base est un TIRAGE PAR
%    GRAPPES (grappe = ticker), pas un échantillon i.i.d.
%  - Mesure : corrélation intra-grappe (ICC), estimée par la
%    décomposition inter/intra de l'ANOVA :
\[
  \hat\rho \;=\; \frac{\mathrm{MSB}-\mathrm{MSW}}
                      {\mathrm{MSB} + (n_0 - 1)\,\mathrm{MSW}},
\]
%    avec MSB / MSW les carrés moyens inter / intra-titres et n_0 la
%    taille moyenne ajustée des grappes.
%  - Lecture du tableau 2.2 : rho quasi nul pour les stratégies à gros
%    effectifs, mais jusqu'à 0,134 (rsi_strict) : les trades d'un même
%    titre y votent partiellement « en bloc ».

% --- Canal 2 : les vagues simultanées --------------------------------
\subsection{Canal 2 --- les vagues simultanées et le facteur marché}
\label{sec:dep-canal2}

% [À RÉDIGER] Idées :
%  - Deux trades ouverts le même jour partagent les mêmes journées de
%    bourse. Or les signaux se déclenchent EN VAGUES : un jour de krach
%    fait sonner le RSI sur des dizaines de titres à la fois.
%  - La cause commune se mesure : corrélation moyenne entre secteurs
%    = 0,70 (hors diagonale) -> le marché bouge largement « d'un bloc ».
%  - ACP des rendements sectoriels (figure 2.1) : la première composante
%    capte 73 % de la variance, TOUS les secteurs de même signe -> c'est
%    le FACTEUR MARCHÉ (risque systématique). La deuxième (8 %) oppose
%    secteurs défensifs et cycliques.
%  - Conclusion du canal : la « cause commune » derrière les trades
%    simultanés n'est pas une hypothèse, c'est une mesure : 73 % des
%    mouvements sont portés par une force unique.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.78\linewidth]{etape3_acp.png}
  \caption{ACP des rendements sectoriels~: la première composante
  (73~\% de la variance, secteurs tous de même signe) est le
  \emph{facteur marché}.}
  \label{fig:acp-marche}
\end{figure}

% --- Canal 3 : les chevauchements dans le temps ----------------------
\subsection{Canal 3 --- les chevauchements dans le temps}
\label{sec:dep-canal3}

% [À RÉDIGER] Idées :
%  - Les trades DURENT : médiane 70 jours, moyenne ~137, et 74 % des
%    trades dépassent un mois. Deux mois voisins partagent donc des
%    trades encore ouverts -> même agrégées par mois, les observations
%    se ressemblent d'un mois sur l'autre.
%  - Mesure : autocorrélation d'ordre 1 de la série des moyennes
%    mensuelles d'edge, rho(1) jusqu'à 0,56 (ma_crossover), cf.
%    tableau 2.2.

\begin{table}[H]
\centering
\caption{Les dépendances mesurées, stratégie par stratégie~:
corrélation intra-titre $\hat\rho$ (canal~1) et autocorrélation
mensuelle $\hat\rho(1)$ (canal~3).}
\label{tab:dep-mesures}
\small
\begin{tabular}{@{}lrr@{}}
\toprule
Stratégie & $\hat\rho$ intra-titre & $\hat\rho(1)$ mensuelle \\
\midrule
\texttt{db\_bottom}    & $0{,}000$ & $0{,}36$ \\
\texttt{dt\_top}       & $0{,}000$ & $0{,}39$ \\
\texttt{hs\_classic}   & $0{,}087$ & $0{,}15$ \\
\texttt{hs\_inverse}   & $0{,}001$ & $0{,}31$ \\
\texttt{ma\_crossover} & $0{,}003$ & $0{,}56$ \\
\texttt{rsi\_classic}  & $0{,}000$ & $0{,}23$ \\
\texttt{rsi\_strict}   & $0{,}134$ & $0{,}03$ \\
\texttt{rsi\_trend}    & $0{,}010$ & $0{,}20$ \\
\texttt{sr\_breakdown} & $0{,}051$ & $0{,}11$ \\
\texttt{sr\_breakout}  & $0{,}004$ & $0{,}05$ \\
\bottomrule
\end{tabular}
\end{table}

% [À RÉDIGER — conclusion de section, 3-5 lignes] Idées :
%  - Bilan : les trois canaux existent et sont mesurés. Aucun n'est
%    assez fort pour crever les yeux (rho intra-titre souvent ~0), mais
%    le chapitre suivant montrera qu'ils suffisent à renverser des
%    verdicts.
%  - Transition : on peut maintenant tester — en sachant exactement ce
%    qui menace les tests.
