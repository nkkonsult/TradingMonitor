"""Stratégies de détection. Chacune expose detect_trades(df, ...) -> list[Trade].

Pour ajouter l'épaule-tête-épaule plus tard : créer head_shoulders.py avec la même
signature detect_trades(...). Le reste de la chaîne (backtest/stats/render) ne change pas.
"""
